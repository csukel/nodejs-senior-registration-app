{
  "name": "seniorschoolregistrationapp",
  "version": "1.0.0",
  "description": "Web App for registering new students",
  "main": "server/app.js",
  "scripts": {
    "start" : "nodemon --inspect server/app.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha --exit server/**/*.test.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "author": "Loucas Stylianou",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^3.0.1",
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.3.0",
    "mysql": "^2.16.0"
  },
  "devDependencies": {
    "expect": "^23.6.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.18.4",
    "supertest": "^3.3.0"
  }
}
